import { NomeComEspacoDirective } from './nome-com-espaco.directive';

describe('NomeComEspacoDirective', () => {
  it('should create an instance', () => {
    const directive = new NomeComEspacoDirective();
    expect(directive).toBeTruthy();
  });
});
